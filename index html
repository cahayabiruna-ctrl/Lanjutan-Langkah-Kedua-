<!DOCTYPE html><html lang="id">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>DVTEAM – Teknik Mengundang</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;700&display=swap" rel="stylesheet">
  <style>
    :root {
      --gold: #ffd166;
      --text: #f0f4f8;
      --muted: #9aa6b8;
      --slide-bg: #1c1f26;
    }
    * { box-sizing: border-box; margin: 0; padding: 0; }
    html, body {
      height: 100%;
      margin: 0;
      background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
      background-attachment: fixed;
      overflow-x: hidden;
      overflow-y: auto;
      padding-bottom: env(safe-area-inset-bottom);
    }body {
  font-family: "Poppins", sans-serif;
  color: var(--text);
  background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
  padding-bottom: env(safe-area-inset-bottom);
}

/* header pojok kanan atas */
.header {
  position: fixed;
  top: 15px;
  right: 20px;
  text-align: right;
  font-size: 0.9rem;
  line-height: 1.3;
  z-index: 10;
  color: var(--muted);
}
.header .brand {
  font-weight: 700;
  color: var(--gold);
}

/* slide layout */
.slide {
  width: 100vw;
  min-height: 100dvh; /* stabil di mobile dengan safe area */
  display: none;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  padding: 2rem 1rem calc(2rem + env(safe-area-inset-bottom)) 1rem;
  box-sizing: border-box;
}
.slide.active { display: flex; }

.content {
  background: var(--slide-bg);
  padding: 2rem 2.5rem;
  border-radius: 20px;
  width: 95%;
  max-width: 1000px;
  height: auto;
  max-height: none; /* biar bisa scroll kalau panjang */
  margin-top: 40px;
  overflow-y: auto;
  box-shadow: 0 10px 30px rgba(0,0,0,0.6);
  animation: fadeIn 0.8s ease;
  position: relative;
  font-size: 0.95rem;
}

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(30px); }
  to { opacity: 1; transform: translateY(0); }
}

h1, h2 { color: var(--gold); margin-bottom: .8rem; }
p, li { line-height: 1.6; margin-bottom: .5rem; }
b { color: var(--gold); }
ul, ol { margin-left: 1.2rem; margin-bottom: 1rem; }

.emoji {
  position: absolute;
  font-size: 1.8rem;
  opacity: 0.7;
  pointer-events: none;
  animation: float 4s ease-in-out infinite;
}
@keyframes float {
  0% { transform: translateY(0) rotate(0deg); }
  50% { transform: translateY(-10px) rotate(10deg); }
  100% { transform: translateY(0) rotate(0deg); }
}
.emoji.bounce {
  animation: bounce 2s infinite;
}
@keyframes bounce {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-15px); }
}

  </style>
</head>
<body>  <div class="header">
    Dipersembahkan oleh:<br>
    <span class="brand">DV-Team Naga Persada</span>
  </div>  <!-- slides -->  <div class="slide active">
    <div class="content">
      <h1>🎯 Teknik Mengundang DVTEAM</h1>
      <p><b>Ringkas</b> — <b>Sederhana</b> — <b>Bikin penasaran</b></p>
      <p>Tujuan undangan: membuat orang hadir, bukan menjelaskan detail lewat chat.</p>
      <div class="emoji" style="top:10px; right:10px;">🚀</div>
      <div class="emoji bounce" style="bottom:10px; left:15px;">✨</div>
    </div>
  </div>  <!-- slides lainnya tetap sama -->  <script>
    let current = 0;
    const slides = document.querySelectorAll(".slide");
    let fullscreenRequested = false;

    function showSlide(i) {
      slides.forEach((s, idx) => s.classList.toggle("active", idx === i));
    }

    function enterFullscreen() {
      if (document.documentElement.requestFullscreen) {
        document.documentElement.requestFullscreen();
      } else if (document.documentElement.webkitRequestFullscreen) {
        document.documentElement.webkitRequestFullscreen();
      } else if (document.documentElement.msRequestFullscreen) {
        document.documentElement.msRequestFullscreen();
      }
    }

    document.body.addEventListener("click", () => {
      if (!fullscreenRequested) {
        fullscreenRequested = true;
        enterFullscreen();
      } else {
        current = (current + 1) % slides.length;
        showSlide(current);
      }
    });

    document.body.addEventListener("keydown", (e) => {
      if (e.key === "ArrowRight") {
        current = (current + 1) % slides.length;
      } else if (e.key === "ArrowLeft") {
        current = (current - 1 + slides.length) % slides.length;
      }
      showSlide(current);
    });
  </script></body>
</html>
